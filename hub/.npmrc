# Refuse installs under a Node that violates package.json engines (>=22).
# Installing under Node 20 compiles better-sqlite3 for NODE_MODULE_VERSION 115
# while the service runs Node 22 (127) -> ERR_DLOPEN_FAILED crash loop.
engine-strict=true
