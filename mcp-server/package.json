{
  "name": "hub-mcp-server",
  "version": "1.0.0",
  "description": "MCP bridge for PolarCopilot Hub Web — replaces curl/Shell polling with native MCP long-poll",
  "type": "module",
  "private": true,
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "zod": "^3.25.0"
  }
}
